/* Cloudflare Pages Redirect Rules */

# Redirect HTTP to HTTPS (Cloudflare handles this automatically, but keep for reference)
# http://:domain/* https://:domain/:splat 301

# Redirect old URLs if needed (add here as necessary)
# /old-article /artikel/islami/new-article 301

# 404 Error Handling
/* /404.html 404

# Security.txt
/.well-known/security.txt /.well-known/security.txt 200

# Ensure XML files are served correctly
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/feed.xml /feed.xml 200
